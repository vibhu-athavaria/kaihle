# M2 Implementation Plan — Gap Map & Teacher Dashboard

## Dependency Graph

```
main
└── M2-1-T1  feat/M2-1-T1_gap-map-service         [root — aggregation logic]
    └── M2-1-T2  feat/M2-1-T2_gap-map-routes       [branches from T1 — replaces stubs]
        ├── M2-1-T3  feat/M2-1-T3_teacher-heatmap  [branches from T2 — teacher UI]
        ├── M2-1-T4  feat/M2-1-T4_student-gap-ui   [branches from T2 — student UI]
        ├── M2-2-T1  feat/M2-2-T1_my-students-list  [branches from T2 — class roster]
        └── M2-2-T2  feat/M2-2-T2_student-profile   [branches from T2 — student profile]
```

**Execution Order:** T1 → T2 → (T3 ‖ T4 ‖ T1 ‖ T2) — sequential per task file, each as subagent

**Conflict Risk:** None identified — each task operates on distinct files

---

## Task Files to Read Before Each Task

| Task | File |
|---|---|
| M2-1-T1 | `docs/milestones/M2/M2-1-T1_gap_map_service.md` |
| M2-1-T2 | `docs/milestones/M2/M2-1-T2_gap_map_routes.md` |
| M2-1-T3 | `docs/milestones/M2/M2-1-T3_gap_map_heatmap_ui.md` |
| M2-1-T4 | `docs/milestones/M2/M2-1-T4_student_gap_profile_ui.md` |
| M2-2-T1 | `docs/milestones/M2/M2-2-T1_my_students_list_ui.md` |
| M2-2-T2 | `docs/milestones/M2/M2-2-T2_student_profile_ui.md` |

---

## Execution Workflow (per task)

1. Read task file
2. Read `CLAUDE.md` + `claude.local.md` for context
3. Launch `general` subagent to implement the task
4. Verify: run tests + linters
5. Branch, commit, push, open PR
6. Self code review with `code-review` skill
7. Wait for CI (non-E2E only)
8. Do NOT merge — leave PR open
9. Move to next task

---

## Merge Strategy (for end)

**PRs in dependency order:**

1. `feat/M2-1-T1_gap-map-service` — base service (no migration)
2. `feat/M2-1-T2_gap-map-routes` — replaces stubs
3. `feat/M2-1-T3_teacher-heatmap` — teacher UI
4. `feat/M2-1-T4_student-gap-ui` — student UI
5. `feat/M2-2-T1_my-students-list` — class roster
6. `feat/M2-2-T2_student-profile` — student profile

**Merge command (human executes):**
```bash
gh pr merge <PR-number> --squash --delete-branch
git checkout main && git pull origin main
```

No migrations in M2 — no `alembic upgrade head` needed between merges.

---

## Key Files Touched (predicted)

| Task | Files |
|---|---|
| M2-1-T1 | `backend/app/services/gap_map_service.py`, `backend/app/schemas/gap_map.py` |
| M2-1-T2 | `backend/app/api/v1/routes/gap_map.py` |
| M2-1-T3 | `frontend/apps/teacher/src/components/gap-map/` |
| M2-1-T4 | `frontend/apps/student/src/pages/` |
| M2-2-T1 | `frontend/apps/teacher/src/pages/MyStudents.tsx` |
| M2-2-T2 | `frontend/apps/teacher/src/pages/StudentProfile.tsx` |

---

## Readiness Check

- [x] Read M2 brief
- [x] Read claude.local.md workflow
- [x] Created dependency graph
- [x] Listed all task files
- [x] Outlined merge strategy

**Awaiting human confirmation before starting implementation.**
